Hyperliquid Scanner Rules V2 - Smoke Test Report
Date: 2026-04-18

Checks performed:
1. Syntax validation passed for:
   - index.js
   - telegram.js
   - state.js
   - dryrun.js
   - signals.js
   - telegramMessageBuilder.js
   - hyperliquid.js
   - strategyMaintenance.js

2. Logic smoke tests passed in a temporary copied workspace with local stubs for Hyperliquid metadata and mids:
   - default profile/bootstrap storage creation
   - default runtime mode seeding (`SIMPLE` + `DEMO`)
   - dynamic pair storage without `pair.js` runtime filtering
   - strict base timeframe filter (`1m` / `5m`)
   - minimum support timeframe filter
   - repeat allocation logic (100% -> 25%)
   - demo trade creation and mode-tagged state persistence
   - mode lock detection with an open trade
   - strategy export/import round trip

3. Verification commands run successfully:
   - `npm run check`
   - `node smoke_test.js` in a temp copy

Notes:
- The smoke test avoids live exchange/network calls by stubbing Hyperliquid pair validation and mids.
- Live REAL-mode trading, admin approval messaging, and exchange reconciliation still need end-to-end validation in the target runtime environment with real credentials.
